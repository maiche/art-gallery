<%= link_to 'Back', gallery_path(@gallery.id) %>

<h2><%= @gallery.title %></h2>
<h3>Exhibition room</h3>

<% if user_signed_in? && current_user.id == @gallery.user_id %>
  <h3>
    <%= link_to 'Edit room', edit_gallery_room_path(@gallery.id,@room.id) %>
  </h3>
<% end %>

<div class="exhibition_room">
  <%= render partial: 'artwork', collection: @artworks %>
</div>
